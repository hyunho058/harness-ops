# build_order.md — <project / batch name>

> The cross-feature ledger for an unattended multi-feature build. Generated by
> `/build-order` from a set of feature specs, approved by a human once, then used as
> the durable resume anchor for the whole run. Copy this, let the skill fill it,
> get it approved, then run. Written atomically (temp + rename).

- generated_at: <stamp — set by the caller>
- input_ref: <fingerprint of the input spec set — Goal lines + spec paths>

## Features  (topological order — a dependency must appear before its dependents)

### <feature-id>
- spec: <path/to/specs/<feature>/spec.md>
- depends_on: []                 # list of feature-ids; empty = root
- status: pending                # pending | in-progress | done | parked
- reason: —                      # escalated | no-spec | error  (only when parked)
- accept: <one-line acceptance seed — what "done" means for this feature>

### <feature-id-2>
- spec: <path/to/specs/<feature-2>/spec.md>
- depends_on: [<feature-id>]
- status: pending
- reason: —
- accept: <...>

<!--
Notes:
- `blocked` is never written here — it is computed each cycle (a depends_on unmet or
  transitively parked). The ready set = pending features whose every depends_on is done.
- `parked` is sticky: only a human edits it back to `pending` after fixing the cause.
- On resume, the skill reconciles this ledger against the current spec set (additive
  changes auto-merge; structural changes require re-approval).
-->
