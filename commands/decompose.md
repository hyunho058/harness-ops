---
description: Turn ONE project goal into N coherent, sibling-aware specs/<feature>/spec.md files — the optional MAKER front door. Derives a partition (disjoint declared-surface globs + depends_on + shared decisions), writes an atomic partition-manifest, runs an overlap pre-flight, gates ONCE with the human, drives specify in mode:batch per feature, then audits its own output via coherence-audit. Reuses specify; never reimplements it; never auto-resolves a conflict.
argument-hint: "\"<one project goal>\""
allowed-tools: [Read, Write, Bash, Glob, Grep, AskUserQuestion, Skill]
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/decompose/SKILL.md` and follow its instructions exactly.

User arguments: $ARGUMENTS
