---
description: Analyzes tasks to propose and execute optimal orchestration patterns — orchestrates sequential/parallel/team/ralph-loop agents, then runs a post-execution verification gate for spec-originated work
argument-hint: "[task description]"
allowed-tools: [Read, Grep, Glob, Bash, Write, Edit, Agent, Task, AskUserQuestion, Skill, SendMessage, TeamCreate, TeamDelete]
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/agent-orchestrate/SKILL.md` and follow its instructions exactly.

User arguments: $ARGUMENTS
